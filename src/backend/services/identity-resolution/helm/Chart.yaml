apiVersion: v2
name: insight-identity-resolution
description: Insight Identity Resolution (epic #1602)
version: 0.2.0
# appVersion is managed by the release pipeline (build-images.yml): it is
# bumped to the freshly built image tag on merges that change this service.
appVersion: "2026.09.06.06.57-d7f073d"
type: application
